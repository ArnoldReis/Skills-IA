/**
 * Metadata de compartilhamento.
 */
export const RETRO_METADATA = {
  sharedWith: ['Engineering', 'Product', 'QA'],
  lessonLearned: 'Always test rollback strategy in staging.', // Why: O compartilhamento evita que duas squads cometam o mesmo erro.
  actionItemsStatus: 'In Progress'
};
