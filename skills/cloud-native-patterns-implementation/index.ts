/**
 * Definição de Sidecar (Conceitual).
 */
export const sidecarPattern = {
  mainContainer: 'my-app',
  sidecar: 'logging-agent' // Why: Permite evoluir os agentes de observabilidade e segurança independentemente da aplicação.
};
